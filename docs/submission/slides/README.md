# スライド（First Stage 2分プレゼン・16:9）

- `slides.html` … 原稿（編集はここ）。8枚、1280×720px
- `slides.pdf` … 提出用PDF（16:9）。`build_pdf.py` で再生成
- `png/slideNN.png` … 各スライド画像（確認・動画用）
- 画面キャプチャは `../captures/`（`poc/scripts/capture.py` で再取得）

```bash
# 再生成（Playwright入りのPython環境で）
/Users/shimadakoutaro/shoken/.venv/bin/python docs/submission/slides/build_pdf.py
```

差し替え予定：スライド1の写真（8/22現地撮影）、来島者数の最新値、デモURL。
